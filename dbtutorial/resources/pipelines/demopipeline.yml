resources:
  pipelines:
    pipeline_demopipeline:
      name: demopipeline
      libraries:
        - glob:
            include: ../../src/pipelines/practice_pipeline/transformations/**
      schema: bronze
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog_name}
      serverless: true
      

         